name: forge2d_samples
description: Flame Forge2D (box2d) samples

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flame_forge2d:
    path: ../
  dashbook: ^0.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/images/pizza.png
