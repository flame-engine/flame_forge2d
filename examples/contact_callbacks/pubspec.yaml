name: forge2d_contact_callbacks
description: Flame sample game showcasing the structure for forge2d games

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flame:
    git:
      url: git@github.com:flame-engine/flame.git
      ref: v1.0.0
  flame_forge2d:
    path: ../../

dev_dependencies:
  flutter_test:
    sdk: flutter
